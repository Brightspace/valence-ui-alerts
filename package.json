{
  "name": "vui-alerts",
  "version": "0.0.11",
  "description": "Mixins and CSS for styling alerts",
  "scripts": {
    "clean": "rimraf error.css && rimraf error-less.css",
    "lint:css": "csslint error.css",
    "prebuild": "npm run clean",
    "build:less": "lessc error.css.less > error-less.css",
    "build:sass": "node-sass --output-style expanded error.css.scss error.css",
    "build": "npm run build:sass && npm run build:less",
    "postbuild": "npm run lint:css",
    "pretest": "npm run build",
    "test": "karma start karma.conf.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Brightspace/valence-ui-alerts.git"
  },
  "keywords": [
    "d2l",
    "valence-ui",
    "vui"
  ],
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/Brightspace/valence-ui-alerts/issues"
  },
  "homepage": "https://github.com/Brightspace/valence-ui-alerts",
  "dependencies": {},
  "devDependencies": {
    "csslint": "^0.10.0",
    "jasmine-dom-matchers": "^0.1.0",
    "karma": "^0.12.32",
    "karma-phantomjs-launcher": "^0.1.4",
    "karma-jasmine": "^0.3.5",
    "karma-cli": "0.0.4",
    "less": "^2.5.1",
    "node-sass": "^3.1.1",
    "rimraf": "^2.3.4"
  }
}
